{
  "name": "@agent-workbench/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "CLI entrypoint package. Owns process startup and command routing only.",
  "main": "dist/index.js",
  "bin": {
    "agent-workbench": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agent-workbench/plugin-sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
