{
  "name": "@agent-workbench/config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Layered config loading, resolution, validation, and secret references.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
